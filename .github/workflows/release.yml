name: Release Binaries (已搁置)

# 桌面端功能已暂时搁置开发，此 workflow 仅输出说明信息。
# 详见 FutureDesktopDevelop.md

on:
  push:
    tags:
      - 'v*'

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: 桌面端构建已搁置
        run: |
          echo "============================================"
          echo " 桌面端（macOS/Windows 托盘）功能已暂时搁置"
          echo " 当前版本不提供桌面二进制 Release"
          echo " 请使用 Docker 镜像或从源码编译（无 desktop tag）"
          echo " 详见 FutureDesktopDevelop.md"
          echo "============================================"
