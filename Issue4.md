# Issue4.md — FWAlizer 问题追踪（当前）

> **文档定位：** 本文档是 FWAlizer 的**当前问题记录**（记录错误与修复方案，非强制，经验参考），承接已存档的 [Issue1-3](HistoryDocs/)（见 `HistoryDocs/`，仅核查）。
> 设计记录见 [Design4.md](./Design4.md)；构建方案见 [Build5.md](./Build5.md)；编码指令见 [AGENTS.md](./AGENTS.md)（**唯一强要求**）。

---

## 一、进行中问题

当前无进行中问题（截至 2026-08-02，Build4 Step 1-13 全部验收通过，全量回归 `go test ./... -race` + 前端生产构建全绿）。

---

## 二、已知遗留 / 候选事项（待规划，非缺陷）

| # | 事项 | 说明 | 来源 | 状态 |
|---|------|------|------|------|
| 1 | DNS 失败写入历史记录 | `EventDNSFailed` 不落库；可写入 `sync_logs`（`result=failed`、`error`=DNS 错误），提升故障可见性 | Design3 §13.4-A | 待规划 |
| 2 | 日志页「暂停输出」按钮 | 高日志量时前端可暂停渲染（性能考虑） | Design3 §13.4-B | 待规划 |
| 3 | 主题设置项加入全局设置页 | 目前主题仅侧边栏切换；复用 `theme` 键即可 | Design3 §13.4-C | 待规划 |
| 4 | 运行测试「上次执行」持久化 | 刷新页面后结果丢失（当前为内存态） | Design4 §三-4 | 待规划 |
| 5 | 部分成功语义说明 | 重试耗尽失败场景（如删除成功、创建失败）历史记录显示 failed 且计数为 0，云端可能有部分变更（下轮同步修正） | Build4 核验记录 | 已知语义，不改 |

---

## 三、格式说明（新问题记录模板）

后续发现问题时，按以下结构追加到「进行中问题」：

```
### RXX-01 问题标题

- **现象：** ...
- **根因：** ...
- **影响范围：** ...
- **修复方案：** ...（决策后同步更新至 Build5 对应 Step）
- **状态：** ☐ 待修复 / ◧ 修复中 / ✅ 已修复（日期 + 验收命令）
```

**流程约定：**

1. 问题发现 → 记录现象/根因/影响范围；
2. 存在方案取舍时，使用提问工具附推荐选项与用户确认；
3. 修复方案确定后，由 [Build5.md](./Build5.md) 承接为构建 Step；
4. 修复完成并验收通过后，更新状态为 ✅ 并记录验收命令与实际结果。

---

## 四、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-08-02 | 文档体系重构：Issue4 创建（承接已存档 Issue1-3），记录已知遗留/候选事项 |
